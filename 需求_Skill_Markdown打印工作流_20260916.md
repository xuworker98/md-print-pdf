# 需求文档 · Skill「md-print-pdf」Markdown 通用打印工作流

**文档版本**　V1.0
**编制日期**　2026-09-16
**文档性质**　立项需求文档（供新会话据此创建 Skill，后期发布至 GitHub）
**目标 Skill 名**　`md-print-pdf`
**GitHub 仓库名**　`md-print-pdf`
**本地项目目录**　`<本地工作区>\40_md-print-pdf\`
**文档状态**　需求已与用户逐条确认定稿，可直接进入实现

---

## 0. 如何使用本文档

本文档是**自包含**的：新会话的执行者看不到需求讨论过程，所有必要信息（技术链路、环境路径、决策结论、踩坑记录、验收标准）均写在此处。

**执行者动作**：读完本文档 → 按第 9 章目录结构创建文件 → 按第 4–7 章实现 → 按第 10 章自检 → 交付。

**命名规范**：本目录下所有新增文件按 `分类_文件名_日期` 命名（如 `需求_Skill_Markdown打印工作流_20260916.md`）；Skill 自身的标准文件（`SKILL.md`、`README.md`、`LICENSE`）为固定名，不受此约束。

---

## 1. 项目定位

### 1.1 一句话定位

把任意 Markdown 文档，稳定打印成**版式可控、可分页、带封面与目录**的 A4 PDF——面向工程文档场景的通用发布工具。

### 1.2 适用场景

- 工程/技术调研报告的正式交付（含图表、代码、公式）
- 内部决策材料的打印稿
- 需要「同一份 md，输出外观一致的 PDF」的场合
- 需要在离线环境出 PDF 的场合（不依赖在线服务）

### 1.3 设计原则

1. **单一职责**：只解决「md → 打印级 PDF」，不做编辑器、不做 Markdown 预览器、不做格式转换器的全家桶。
2. **零网络依赖**：全部依赖本机已有资源（系统浏览器 + 托管 Node），不下载任何东西即可运行（数学公式除外，见 §6.11）。
3. **配置驱动**：所有版式参数外置为配置文件 / 命令行参数，不改代码即可换样式。
4. **分页优先**：宁可牺牲版面紧凑度，也不允许出现「图片被拦腰切断」「标题孤悬页尾」「跨页表格无表头」。
5. **可复现**：同一输入 + 同一配置 = 同一输出，不引入随机因素。

---

## 2. 技术链路（含为什么这样选）

### 2.1 主链路

```
Markdown 源文件
    │
    ├─[步骤1]─> 零依赖 md2html 转换器（Python）
    │              · 解析 md 语法 → HTML 结构
    │              · md 表格加 class="md"（走边框样式）
    │              · md 内嵌 HTML 表格保持无边框（并排图靠它）
    │              · <img> 单行必须包成 <p class="fig">（见 §8 坑位1）
    │              · 图片路径 → file:/// 绝对 URI（中文需百分号编码）
    │              · 注入 A4 打印 CSS
    │
    ├─[步骤2]─> A4 打印版 HTML（中间产物）
    │
    ├─[步骤3]─> playwright-core 驱动系统 Edge → page.pdf()
    │              · 走 stdio pipe（--remote-debugging-pipe），不用网络
    │              · 原生支持中文页脚模板（pageNumber / totalPages）
    │              · 输出 A4 纵向 PDF
    │
    └─[步骤4]─> 质检（PDF 结构解析 + 逐页截图目视）
```

### 2.2 为什么不用其他方案（重要，避免走回头路）

> ⚠️ 以下均为**实测结论**，不是推测。执行者不要再尝试这些路径。

| 方案 | 实测结果 | 原因 |
|---|---|---|
| **Edge `--remote-debugging-port` + 自写 CDP 客户端** | ❌ 失败 | 本机沙箱**拦截 loopback TCP**：端口不监听、`DevToolsActivePort` 文件不生成、连接超时。用 Python 手写 WebSocket 客户端亦无法通信 |
| **Edge 命令行 `--print-to-pdf`** | 🔶 可用但**功能不足** | 能出 PDF，但**无法自定义页脚模板**（CLI 不支持 `footerTemplate`），拿不到「第 N 页 / 共 M 页」 |
| **Pandoc + LaTeX** | ❌ 不可行 | 需约 1GB TeX 发行版；本机 `pip install` 全部超时（代理 `127.0.0.1:12198` 不通），无法安装 |
| **Python `markdown` / `reportlab` / `pypdf` / `weasyprint`** | ❌ 不可用 | 均未安装，且网络不通无法安装 |
| **pdfkit-py 插件 skill** | 🔶 未验证 | 需先跑 `setup.bat` 建 venv 并从 CDN 下字体，**同样受网络阻塞** |
| **在线转换服务** | ❌ 排除 | 违背「离线可用」原则，且工程文档涉密不宜外传 |

**正解**：`playwright-core` **已预装**在托管 Node 工作区，它走 `--remote-debugging-pipe`（stdio 管道）与浏览器通信，**完全绕开网络限制**。

---

## 3. 环境依赖与调用范式

### 3.1 依赖清单（均为本机已有，无需安装）

| 依赖 | 绝对路径 | 用途 |
|---|---|---|
| Node.js 22.22.2 | `<用户目录>\.workbuddy\binaries\node\versions\22.22.2-3\node.exe` | 运行打印脚本 |
| playwright-core | `<用户目录>\.workbuddy\binaries\node\workspace\node_modules\playwright-core` | 驱动浏览器，stdio 通信 |
| Microsoft Edge | `C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe` | 渲染内核（实测版本 153.0.4234.32） |
| Python 3.13.12 | `<用户目录>\.workbuddy\binaries\python\versions\3.13.12\python.exe` | 运行 md2html 转换器 |

### 3.2 调用范式（**必须遵守**）

**Node 脚本调用**（关键：必须设 `NODE_PATH`，否则找不到模块）：

```bash
NODE_PATH="<用户目录>/.workbuddy/binaries/node/workspace/node_modules" \
"<用户目录>/.workbuddy/binaries/node/versions/22.22.2-3/node.exe" \
  scripts/print_pdf.js --html <输入html> --pdf <输出pdf> [其他参数]
```

**Python 脚本调用**：

```bash
"<用户目录>/.workbuddy/binaries/python/versions/3.13.12/python.exe" \
  scripts/md2html.py --md <输入md> --out <输出html> --config <配置json>
```

**跨平台注意事项**：本机 Python 路径为托管环境专用。Skill 在**其他机器**上运行时，须改为自行探测：优先 `python3`/`python`，Node 优先 `node`，playwright-core 优先项目内 `node_modules`。**脚本必须实现依赖探测与友好报错**。

### 3.3 关键 API 片段（打印核心）

```javascript
const { chromium } = require('playwright-core');

const browser = await chromium.launch({
  executablePath: process.env.EDGE_PATH || '<按 §3.1 探测>',
  headless: true,
  args: ['--no-sandbox', '--disable-gpu', '--allow-file-access-from-files'],
});
const page = await browser.newPage();
await page.goto('file:///...', { waitUntil: 'load' });
await page.waitForTimeout(1500);        // 等图片加载

await page.pdf({
  path: outPdf,
  format: 'A4',
  printBackground: true,
  displayHeaderFooter: true,             // 开启页眉页脚
  headerTemplate: '<div></div>',         // 页眉留空
  footerTemplate: '<div style="...">第 <span class="pageNumber"></span> 页 / 共 <span class="totalPages"></span> 页</div>',
  margin: { top: '14mm', bottom: '18mm', left: '15mm', right: '15mm' },
});
```

> **要点**：`pageNumber` / `totalPages` 是浏览器内核私有 CSS 类，**只能在页眉页脚模板里使用**，不能注入正文——这是「真页码目录无法实现」的根本原因（见 §6.10）。

---

## 4. 字体规范（已定稿）

### 4.1 字体族与字重矩阵

| 元素 | 字体族 | 字重 | 说明 |
|---|---|---|---|
| H1 / H2 / H3 / H4 标题 | **黑体** SimHei | **加粗** | 标题层唯一加粗主体 |
| 表头单元格（`<th>`） | **宋体** SimSun | **加粗** | 表头与数据行需区分 |
| 正文中带 `**标记**` 的内容 | 宋体 | **加粗（保留）** | **忠于 md 源文件**，不擅自增删字重 |
| 正文普通文字 | 宋体 | 不加粗 | |
| 表格名 / 图片名（表注图注） | 宋体 | 不加粗 | |
| 英文、数字（所有位置） | **Times New Roman** | 随所在元素 | 与中文混排时英文走 TNR |
| 代码块 | 等宽字体（Consolas / 宋体回退） | 不加粗 | 保留原始空格与缩进 |

### 4.2 字体回退链（**必须实现**）

```css
/* 中文标题 */
font-family: "SimHei", "黑体", "Microsoft YaHei", "微软雅黑", sans-serif;
/* 中文正文 */
font-family: "SimSun", "宋体", "Microsoft YaHei", "微软雅黑", serif;
/* 英文数字（与中文混排，用 unicode-range 或置于中文之前） */
font-family: "Times New Roman", "SimSun", "宋体", serif;
```

### 4.3 字体可用性预检（功能项 #1，**必须实现**）

**行为要求**：
1. 启动时检测 `C:\Windows\Fonts\` 下 `simhei.ttf` / `simsun.ttc` / `times.ttf` 是否存在；
2. 任一缺失 → 走回退链，并在**质检报告**中输出告警，格式：
   ```
   [字体告警] 未找到 SimHei（黑体），标题将回退为 Microsoft YaHei。
              原因：目标机器未安装该字体。建议：安装字体或改用可嵌入的替代字体。
   ```
3. **禁止静默降级**——用户必须知道字体被替换了。
4. 支持通过配置项 `fonts.*` 显式指定字体路径（用于打包字体文件的场景）。

> **跨平台**：Linux/macOS 下应探测 `fc-list` 或常见路径（如 `/System/Library/Fonts/`），并提示安装「思源宋体/黑体」等开源替代。

---

## 5. 配置项清单（全部可外部设置）

配置文件格式：JSON（默认 `print.config.json`，可用 `--config` 覆盖）。**所有项均有默认值**，不配置即可运行。

### 5.1 页面层

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `paper.size` | `"A4"` | 纸张：A4 / A3 / Letter |
| `paper.orientation` | `"portrait"` | portrait / landscape |
| `margin.top` | `"14mm"` | |
| `margin.bottom` | `"18mm"` | 需给页脚留位置 |
| `margin.left` / `margin.right` | `"15mm"` | |

### 5.2 版式层

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `typography.baseFontSize` | `"10.4pt"` | 正文字号 |
| `typography.lineHeight` | `1.74` | 行距 |
| `typography.textAlign` | `"justify"` | justify / left |
| `typography.h1Size` | `"19pt"` | |
| `typography.h2Size` | `"13.5pt"` | |
| `typography.h3Size` | `"11.4pt"` | |
| `typography.tableFontSize` | `"8.7pt"` | 表格字号 |
| `pageBreak.chapterOnNewPage` | `true` | **每章另起页开关**（功能项 #4） |

### 5.3 页眉页脚

| 配置项 | 默认值 | 可选值 |
|---|---|---|
| `headerFooter.preset` | `"title-pagefooter"` | `none` / `pagenum-only` / `title-pagefooter` / `chapter-pagefooter`（功能项 #11 四套预设） |
| `headerFooter.docTitle` | 从 md 一级标题自动取 | 页脚显示的文档名 |
| `headerFooter.footerFormat` | `"第 {page} 页 / 共 {total} 页"` | 支持 `{page}` `{total}` 占位符 |
| `headerFooter.fontSize` | `"12px"` | |
| `headerFooter.color` | `"#6b7280"` | |

### 5.4 封面（功能项 #12）

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `cover.enabled` | `true` | 是否生成封面 |
| `cover.title` | 从 md 一级标题取 | |
| `cover.subtitle` | `""` | |
| `cover.version` | `""` | 如 `V2.0` |
| `cover.date` | 当天日期 | |
| `cover.org` | `""` | 编制单位 |
| `cover.classification` | `""` | 密级，如「内部资料」 |
| `cover.template` | `"classic"` | 版式预设：classic / minimal / banner |

### 5.5 目录（功能项 #13、#14）

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `toc.enabled` | `true` | |
| `toc.depth` | `2` | 收录到第几级标题（2 = H1+H2） |
| `toc.linkable` | `true` | 是否可点击跳转 |
| `toc.showPageNumber` | `false` | **固定 false**（技术限制，见 §6.10） |

### 5.6 其他

| 配置项 | 默认值 | 说明 |
|---|---|---|
| `watermark.enabled` | `false` | 水印（功能项 #10） |
| `watermark.text` | `""` | 如「内部资料 请勿外传」 |
| `watermark.opacity` | `0.08` | |
| `metadata.title` / `.author` / `.subject` / `.keywords` | `""` | PDF 元数据（功能项 #8） |
| `bookmarks.enabled` | `true` | PDF 侧边书签（功能项 #9） |
| `math.mode` | `"placeholder"` | `placeholder` / `image`（见 §6.11） |
| `math.imageDir` | `""` | `--math-image` 接口的目录 |
| `fonts.*` | 见 §4.2 | 字体族覆盖 |

---

## 6. 能力边界与功能规格

### 6.1 支持的内容类型（**边界内**）

| # | 类型 | 要求 |
|---|---|---|
| 1 | 普通文本段落 | 两端对齐，首行不缩进（工程文档习惯） |
| 2 | 一至四级标题 | 黑体加粗；H1 强制另起页 |
| 3 | 图片 | 居中；超版心自动缩至 100%；支持 `width` 属性控宽 |
| 4 | 一行多图（并排） | 用 md 内嵌 HTML `<table>` 实现，**该表不加边框** |
| 5 | Markdown 表格 | 加 `class="md"`：表头底色 + 斑马纹 + 细边框 + 跨页重复表头 |
| 6 | 代码块 | 等宽字体、浅灰底、不折行（超宽则缩字号） |
| 7 | 有序列表 / 无序列表 | 正常缩进渲染 |
| 8 | 数学公式 | 见 §6.11（首版占位告警） |
| 9 | 脚注 | **降级为章末/文末尾注**，见 §6.12 |
| 10 | 引用块 | 灰底 + 左边框，用于图注/表注/依据说明（功能项 #13 语义类） |
| 11 | 分隔线 | 细横线 |

### 6.2 明确不支持（**边界外**，须在 README 声明）

| 项 | 原因 |
|---|---|
| **真页码目录** | 浏览器打印引擎无法把页码注入正文（`pageNumber` 仅存在于页眉页脚模板）。详见 §6.10 |
| **当页脚注**（真·页底脚注） | 需手工分页计算，且浏览器无分页 API。降级为章末/文末尾注 |
| **两栏排版** | 图/表跨栏处理复杂，工程文档少用 |
| **横竖版混排** | 需 CSS `@page` 命名页，工程量大。**替代方案**：超宽表自动降字号 + 质检告警，提示用户拆列 |
| **深色/浅色主题切换** | 打印场景固定白底 |
| **公式交叉引用** | MathJax 支持编号，交叉引用需额外脚本。首版只做编号 |
| **CDN 加载公式引擎** | 离线环境下公式会变乱码，比不支持更糟。**禁用 CDN** |

### 6.3 功能项 #1 · 字体可用性预检

见 §4.3。

### 6.4 功能项 #2 · 图表自动编号 + 交叉引用

**要求**：
1. 扫描文档中形如「图 X」「表 X」的图注/表注，**统一重新编号**（可选，配置 `numbering.auto`）；
2. 支持在 md 中用占位符引用（建议语法 `{{fig:历史沿革}}` / `{{tab:需求清单}}`），输出时替换为实际编号；
3. **交叉引用校验**：若正文引用了不存在的图表编号 → 质检报告输出**错误级**告警；
4. 编号风格可配：`图 1` / `图 1-1`（按章编号）/ `Figure 1`。

> **背景**：本项目的前身（某 52 页调研报告）有 38 处图引用，全靠人工维护零错误。此项功能是工程文档最易出错处，**价值最高**。

### 6.5 功能项 #3 · 页眉支持

**要求**：四套预设（见 §5.3）。`chapter-pagefooter` 预设下，页眉显示当前章标题（奇偶页可分别设置）。

### 6.6 功能项 #4 · 章节强制另起页开关

配置 `pageBreak.chapterOnNewPage`。开启时 H1 强制 `break-before: page`；关闭时连续排版。

### 6.7 功能项 #5 · 横竖版混排

**不做**（用户已定）。替代：超宽表自动降字号至 7pt 下限，并在质检报告告警：
```
[表格告警] 第 3 个表格（位于 §4.2）宽度超出页面，已自动缩至 7.5pt。
           建议：拆分列或改用横向页（当前版本不支持）。
```

### 6.8 功能项 #6 · 图片自动缩放 + 防溢出

**要求**：
1. 图片宽度 > 版心 → 自动缩至 100%；
2. 图片高度 > 单页可用高度 → 告警提示「图片将被跨页，建议拆分或缩小」；
3. 检测图片文件是否存在 → 缺失则告警并输出占位框（**禁止静默空白**）。

### 6.9 功能项 #7 · 表格防溢出 + 列宽策略

**要求**：
1. 表格总宽超版心 → 优先降字号（至 7pt 下限），仍超则告警；
2. 单元格内容不折行导致超宽 → `word-break: break-word`；
3. 检测「表格在某列内容为空」→ 输出 `&nbsp;` 防止塌陷（坑位，见 §8）。

### 6.10 功能项 #13/#14 · 自动化目录（**A 方案**）

**已定口径**：`toc.showPageNumber` 固定 `false`。

**要求**：
1. 从 H1/H2（深度可配）抽取生成目录页；
2. 目录项**可点击跳转**（用 HTML 锚点 `href="#sec-1-2"`，PDF 阅读器支持内部跳转）；
3. 层级缩进体现结构；
4. 目录页单独成页，不混入正文。

**技术限制说明（须写入 README）**：

> 本工具生成的目录**不含页码**。原因是：浏览器打印引擎的页码（`pageNumber` / `totalPages`）是内核私有变量，仅能在页眉页脚模板中使用，无法注入正文内容。要生成带真实页码的目录，需在渲染后逐页探测各章位置，工程量为无页码方案的 5–8 倍，且长表/长图会造成 ±1 页误差——目录失信比没有页码更糟。因此本工具选择「无页码 + 可点击跳转」方案。

### 6.11 功能项 · 数学公式（双路方案）

**首版（`math.mode = "placeholder"`）**：

1. md 中的公式语法（`$...$`、`$$...$$`）**原样保留在中间 HTML 中**，用 `<span class="math">` 包裹；
2. 输出 PDF 时，**替换为显式提示**，如：
   ```
   ［公式］E = mc^2　（未渲染：数学公式引擎未激活）
   ```
3. **禁止静默丢弃或渲染成乱码**；
4. 质检报告输出告警，列出公式所在位置与数量。

**预留接口（`math.mode = "image"`）**：

1. 支持 `--math-image <dir>` 参数，或配置 `math.imageDir`；
2. 目录内按公式序号命名图片（`math-001.png`、`math-002.png`…），脚本**自动按出现顺序替换**；
3. 图片位置、尺寸、居中方式与普通图片一致。

**未来激活路径（写入 README）**：

> 本工具不在运行时联网加载公式引擎（离线环境会导致公式变乱码）。如需完整数学公式支持，可任选其一：
> 1. **预处理为图片**：用任意工具（KaTeX 命令行、matplotlib mathtext、Word 公式截图）把公式生成为 PNG，放入目录后用 `--math-image` 指定；
> 2. **下载离线包**：将 KaTeX 或 MathJax 的静态文件放入 `assets/`，并开启配置 `math.engine = "katex-local"`（此选项需自行准备静态资源）。

> **说明**：本机网络不通，无法自动下载离线包；且「公式转图片」同样需要一个公式解析引擎（如 KaTeX 或 matplotlib），**不构成绕开网络的方案**，只是把「运行时依赖」变为「预处理依赖」。

### 6.12 功能项 #16 · 脚注（降级方案）

**已定口径**：**降级为章末/文末尾注**。

**要求**：
1. md 中的脚注语法（`[^1]`）→ 正文处显示上标编号，文末/章末生成注释列表；
2. 注释列表样式：字号小一号、灰色；
3. 质检报告提示「脚注已排在文末，非当页」——让用户知道差异。

### 6.13 功能项 #8 · 文档元数据

**要求**：将 `metadata.*` 写入 PDF 属性（标题/作者/主题/关键词），便于文档检索。

### 6.14 功能项 #9 · PDF 书签（大纲）

**要求**：从 H1/H2 自动生成侧边栏书签树，与目录同源。用 `page.pdf({ outline: true })` 或 Chromium 默认行为。

### 6.15 功能项 #10 · 水印

**要求**：对角线重复水印文字，透明度可配，居中。用于「内部资料 请勿外传」等密级标识。

### 6.16 功能项 #11 · 页眉页脚多套预设

见 §5.3。四套预设：`none` / `pagenum-only` / `title-pagefooter` / `chapter-pagefooter`。

### 6.17 功能项 #12 · 封面模板化

**要求**：可配字段（标题/副标题/版本/日期/单位/密级），三种版式预设（classic / minimal / banner）。封面单独成页，不编入页脚页码（或另编一套罗马数字，可配）。

### 6.18 功能项 #14 · 目录深度可配

见 §5.5。

---

## 7. 排版规则（本工具的核心竞争力）

> 以下四条**必须实现且不可关闭**，是「正式文件」与「网页打印」的分水岭。

| # | 规则 | CSS 实现 | 解决什么问题 |
|---|---|---|---|
| **1** | **图片不被切断** | 图段落 `break-inside: avoid` | 避免图片被拦腰截成两页 |
| **2** | **标题不孤悬页尾** | 标题 `break-after: avoid` + `break-inside: avoid` | 避免标题独自留在页底、正文翻页才开始 |
| **3** | **跨页表格重复表头** | `thead { display: table-header-group }` | 长表格第二页起仍有表头，可读性关键 |
| **4** | **章标题另起页** | H1 `break-before: page`（可配关闭） | 章节分野清晰 |

**附加规则**：
- 表格行 `break-inside: avoid`（避免单行被拆两页）；
- 引用块（图注表注）`break-inside: avoid`；
- MD 表格默认 `break-inside: auto`（长表允许跨页，但表头重复）。

---

## 8. 已知坑位清单（**实测踩过，务必处理**）

| # | 坑位 | 现象 | 正确做法 |
|---|---|---|---|
| **1** | `<img>` 未包 `<p class="fig">` | 单行 HTML 的 `<img>` 作为**行内元素**，会与前后文字挤在同一行 | md 转换时，凡**整行仅含 `<img>`** 的，必须包成 `<p class="fig">` |
| **2** | 并排图表格被加边框 | md 内嵌 HTML `<table>` 若被套用 md 表格样式，并排图会带边框 | md 表格用 `class="md"` 选择器区分；内嵌 HTML 表用 `table:not(.md)` 排除 |
| **3** | 表格空单元格塌陷 | 单元格内容为空时表格可能塌陷 | 空单元格输出 `&nbsp;` |
| **4** | 图片路径含中文 | `file:///` URI 需百分号编码 | 用标准库的路径转 URI 方法（Python: `Path.as_uri()`），勿手工拼字符串 |
| **5** | HTML 行被当正文数字审计 | 质检脚本可能把 `width="50%"` 当正文数字 | 质检规则中**跳过以 `<` 开头的行** |
| **6** | 图注窗口过窄误报 | `<img>` 常处于表格中，上下图注距离较远 | 图注检查窗口对 `<img>` 放宽（如 ±8 行） |
| **7** | `%` 格式化与 HTML 冲突 | Python 字符串 `%` 格式化遇到 `width="50%"` 会抛 `ValueError: unsupported format character` | 含 HTML 的字符串**禁用 `%` 格式化**，改用 f-string 或 `.format()` |
| **8** | 变量替换顺序错误 | 先把 `![]()` 转成 `<img>` 后，后续正则仍按旧写法匹配 → 匹配失败 | 变换链中**每一步后确认下一步的匹配目标形态**；或统一在最后阶段转换 |
| **9** | 批量改写脚本中途失败污染文件 | 脚本跑到一半失败，文件已被部分修改 | **每步断言 `count == 1`**，任一失败立即 `sys.exit(1)` **不落盘**；或先备份 |
| **10** | PDF 无法目视验收 | 无 pypdf 等库，无法解析 PDF 内容 | 用 headless 浏览器 `page.goto('file:///x.pdf')` 打开**内置 PDF 查看器**，截图目视；`#page=N` 锚点可翻页 |
| **11** | PDF 文件过大 | 嵌入高清 PNG 会导致 PDF 数十 MB | 图片预压缩；或提供 `--image-quality` 参数（JPEG 重编码） |
| **12** | 表格基线错位 | md 表格分隔行 `|---|---|` 识别失败会导致整表渲染异常 | 分隔行须严格匹配 `^\|[\s\-:|]+\|$`；表头列数 vs 数据列数不一致时补齐或告警 |
| **13** | `__pycache__` 污染项目目录 | 用 `py_compile` 或普通导入执行脚本，会在脚本同目录生成 `__pycache__`，污染 Git 仓库 | 语法检查用 `python -c "import ast; ast.parse(open(f,encoding='utf-8').read())"`，**禁用 `py_compile`**；运行脚本时工作目录切到临时目录；`.gitignore` 必须含 `__pycache__/`、`*.pyc` |
| **14** | 临时产物落在仓库内 | 中间 HTML、Edge 用户数据目录、空跑截图等若落在仓库根，会污染 Git | 所有中间产物统一写入系统临时目录（可用 `--work-dir` 指定），**默认不写入仓库**；`.gitignore` 覆盖 `*.html`（除 examples）、`.edge-profile/` |

---

## 9. 建议的目录结构（GitHub 仓库根）

```
40_md-print-pdf/
├── 需求_Skill_Markdown打印工作流_20260916.md   ← 本文档（立项输入）
├── README.md               项目说明、快速开始、能力边界、限制声明
├── LICENSE                 开源协议（建议 MIT）
├── SKILL.md                Skill 主文件（frontmatter + 工作流说明）
├── print.config.json       默认配置（对应 §5 全部配置项）
├── scripts/
│   ├── md2html.py          零依赖 md → A4 打印版 HTML
│   ├── print_pdf.js        playwright-core → PDF（含页眉页脚/书签/元数据）
│   ├── qc_check.py         质检（字体预检、图表编号校验、溢出检测、告警汇总）
│   └── run.py              一键编排（读配置 → 转 HTML → 出 PDF → 质检）
├── references/
│   ├── pitfalls.md         §8 坑位清单（含复现与正解）
│   ├── css-rules.md        §7 排版规则详解
│   └── config-reference.md §5 配置项逐条说明
├── assets/
│   ├── cover/              封面模板
│   ├── katex/              （可选）公式引擎离线包，默认空
│   └── fonts/              （可选）字体文件，默认空
└── examples/
    ├── demo.md             示例文档（覆盖全部支持的内容类型）
    └── demo.pdf            示例输出
```

---

## 10. 验收清单（新会话建完 Skill 后逐项自检）

### 10.1 功能验收

- [ ] 一块文本 + 图 + 表 + 代码块 + 有序/无序列表 + 引用块的混合文档，能完整生成 PDF
- [ ] 字体符合 §4.1 矩阵（黑体标题、宋体正文、TNR 英文、表头宋体加粗）
- [ ] 封面按配置生成，字段正确
- [ ] 目录生成，层级正确，**可点击跳转**，无页码
- [ ] 页脚显示「文档名 ｜ 第 N 页 / 共 M 页」，页码连续正确
- [ ] PDF 侧边书签可用
- [ ] PDF 元数据（标题/作者）可查
- [ ] 四套页眉页脚预设均实测通过

### 10.2 排版质量验收（**最关键**）

- [ ] 全文**无图片被跨页切断**
- [ ] 全文**无标题孤悬页尾**
- [ ] **跨页表格每页有表头**
- [ ] 每章另起页（开关开启时）
- [ ] 并排图两图同行、无边框、尺寸正确
- [ ] 超宽表格自动降字号且有告警

### 10.3 健壮性验收

- [ ] 字体缺失时输出告警（可临时改名测试）
- [ ] 图片缺失时输出占位框 + 告警
- [ ] 图表交叉引用错误时输出告警
- [ ] 公式存在时输出「未渲染」提示 + 告警（非乱码、非静默）
- [ ] 空单元格、含中文路径图片、`width="50%"` 均不报错
- [ ] 配置文件缺项时用默认值，不崩溃

### 10.4 发布验收

- [ ] README 含：快速开始、配置说明、**能力边界与限制声明**（尤其「目录无页码」「公式未激活」）
- [ ] examples/demo.md 覆盖全部支持类型
- [ ] LICENSE 就位
- [ ] 跨平台说明（非本机环境下如何探测依赖）

---

## 11. 与既有报告的差异说明（重要）

本规范与既有的《机器人产业与通信网络调研报告 V2.0》版式存在以下**已知差异**，属**用户明确要求的方向变更**：

| 项 | V2.0 报告 | 本 Skill 默认 |
|---|---|---|
| 正文字体 | 微软雅黑 | **宋体** |
| 标题字体 | 微软雅黑加粗 | **黑体加粗** |
| 表头 | 微软雅黑加粗 | **宋体加粗** |
| 封面 | 无 | **有** |
| 目录 | 无 | **有**（无页码 + 跳转） |
| 页眉 | 无 | 可选（默认仅页脚） |
| 正文加粗保留 | 是 | **是**（忠于源文件） |

> **含义**：用本 Skill 重排 V2.0 报告，外观会与当前 PDF 明显不同。这是预期行为。

---

## 12. 遗留与后续

| 项 | 状态 | 说明 |
|---|---|---|
| 数学公式引擎离线包 | **未激活** | 需网络下载 KaTeX/MathJax 静态文件；本机网络不通 |
| 字体文件打包 | 待定 | 若需跨机器一致，需附带开源字体（思源宋体/黑体，注意 License） |
| 真页码目录 | 明确不做 | 见 §6.10 |
| 当页脚注 | 降级 | 见 §6.12 |
| 横竖版混排 | 明确不做 | 见 §6.7 |
| GitHub 发布 | 待执行 | 需补 README/LICENSE/examples |

---

## 附：文档全部决策记录（供追溯）

| # | 议题 | 决策 | 决策方 |
|---|---|---|---|
| 1 | 功能项 1–14 | 全部采纳 | 用户 |
| 2 | 功能项 15 真页码目录 | 不做 | 用户 |
| 3 | 功能项 16 当页脚注 | 降级为章末/文末尾注 | 用户 |
| 4 | 功能项 17 两栏排版 | 不做 | 用户 |
| 5 | 功能项 18 公式编号 | 做编号，不做交叉引用 | 用户 |
| 6 | 功能项 19 主题切换 | 不做，固定白底 | 用户 |
| 7 | 横竖版混排 | 不做 | 用户 |
| 8 | 加粗方案 | 标题黑体加粗；表头宋体加粗；正文保留源文件标记的加粗；其余宋体不加粗 | 用户 |
| 9 | 目录口径 | A 方案（无页码 + 可点击跳转） | 用户 |
| 10 | 公式方案 | 双路：首版占位告警 + 预留 `--math-image` 接口 | 用户 |
| 11 | 字体 | 中文标题黑体、其他中文宋体、英文 Times New Roman | 用户 |
| 12 | 封面 / 目录 | 新增 | 用户 |
| 13 | 落位 | `<本地工作区>\40_md-print-pdf\` | 用户 |
| 14 | Skill 名 / 仓库名 | `md-print-pdf` | 用户 |
| 15 | 发布目标 | GitHub 公开仓库 | 用户 |
